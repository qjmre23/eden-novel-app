<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security config for Eden Novel.
  HTTPS-only: cleartext (HTTP) traffic is blocked everywhere.
  This pairs with android:usesCleartextTraffic="false" in AndroidManifest.xml.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
